import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Changelog — Product Updates | Crawler Que",
  description: "Latest Crawler Que updates: new audit modules, AI visibility improvements, and reporting features. See what shipped each month.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }